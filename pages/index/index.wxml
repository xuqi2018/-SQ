<!--index.wxml-->
<view class="container">
  <view class="container">
    <button bindtap="login">授权并登陆</button>
  </view>
  <view>
    <button wx:if="{hasUserInfo}" opne-type="toMainStage" bindtap="toMainStage">进入首页</button>
  </view>
</view>
